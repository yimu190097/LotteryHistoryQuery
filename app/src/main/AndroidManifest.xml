<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 客服语音消息：录音 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 客服发送付款截图：读取图片（13+用READ_MEDIA_IMAGES，兼容旧版READ_EXTERNAL_STORAGE） -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".LotteryApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.LotteryHistory"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 联系客服（聊天） -->
        <activity
            android:name=".ui.CustomerServiceActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <!-- 语音通话客服（模拟呼叫/通话中，后期接 VoIP 后端） -->
        <activity
            android:name=".ui.VoiceCallActivity"
            android:exported="false" />
    </application>

</manifest>
