## 本轮交付：Lint 0 errors + 追加投注全链路可见 + 开发阶段无条件全量覆盖

### 1. Lint 0 errors（原5 errors全修）
- 3 个 ConstraintLayout 子 View MissingConstraints：
  - dialog_draw_detail.xml: svRules
  - dialog_latest_draws.xml: ScrollView
  - dialog_query_records.xml: rvQueryRecords
  全部补全 layout_constraintTop_toTopOf / Start_toStartOf / End_toEndOf="parent"
- 2 个 DiffUtilEquals（List 接口未声明 equals()）：
  - MainActivity LATEST_DIFF.areContentsTheSame 中 primaryNumbers/secondaryNumbers
    封装 listEquals() 逐一比较元素，不再直接用List == List

### 2. 开发阶段每次都无条件全量拉取+全量覆盖（你明确指令）
- LotteryDataManager.refresh keepLocal=false 全部无条件重写
- 删除所有"增量更新只写新期号""本地MATCH完整就保留"逻辑
- PARSER_VERSION_CURRENT=v3，XlsParser统一引用该常量（不再写死1）

### 3. 追加投注全奖级0注也展示（DrawDetailDialog P0修复）
- hasAppendData 放宽：只要规则版本 appendTierPairCount>0 且 appendEntry非null就true
- 注数列：0注写「追加空开」
- 金额列：0金额写「追加投注 —」
- 每个奖级一定有追加一行，用户再也不会以为DLT没有追加玩法
